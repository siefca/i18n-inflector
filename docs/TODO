* update documentation for InflectionDataStrict[ ] and InflectionData[ ]
* update documentation for API[done] and API_Strict[ ]
* update RELATIONS
* validate error messages for strict kinds (if kind is prefixed by @)


* write some examples in section API Examples

== Near future

* add a switch that causes inflection arguments to be gathered before using original
  version of translate and then injected when interpolating happens (default value
  for that switch should be true if there is I18n version 4 installed)

